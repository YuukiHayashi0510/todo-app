version: "2"
sql:
  - engine: "postgresql"
    queries: "./db/queries/"
    schema: "./db/migrations/*.up.sql"
    gen:
      go:
        package: "rdb"
        out: "./internal/persistence/rdb"
        emit_empty_slices: true
